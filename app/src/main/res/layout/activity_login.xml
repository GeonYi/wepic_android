<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:gravity="center_horizontal"
    android:orientation="vertical" android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" tools:context=".LoginActivity">

    <!-- Login progress -->

    <ScrollView android:id="@+id/login_form" android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:id="@+id/email_login_form"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <EditText
                android:id="@+id/login_text_email"
                android:style="@style/EditText_Green"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dim_50"
                android:hint="@string/prompt_email"
                android:imeOptions="actionUnspecified"
                android:inputType="textEmailAddress"
                android:maxLines="1"
                android:singleLine="true" />

            <EditText
                android:id="@+id/login_text_password"
                android:layout_marginTop="10dp"
                android:style="@style/EditText_Green"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dim_50"
                android:hint="@string/prompt_password"
                android:imeOptions="actionUnspecified"
                android:inputType="textPassword"
                android:maxLines="1"
                android:singleLine="true" />

            <Button
                android:id="@+id/login_button_login" style="@style/Button_Green"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dim_50"
                android:layout_marginTop="20dp"
                android:text="@string/action_login_in"
                android:textStyle="bold" />

            <Button android:id="@+id/login_button_reg" style="@style/Button_Green"
                    android:layout_width="match_parent"
                    android:layout_marginTop="10dp"
                    android:layout_height="@dimen/dim_50"
                    android:text="@string/action_login_reg"
                    android:textStyle="bold" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>



